import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.musicstreamer.front',
  appName: 'MusicStreamerFrontend',
  webDir: 'dist'
};

export default config;
